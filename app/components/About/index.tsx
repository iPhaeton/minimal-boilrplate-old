import * as React from 'react';
import {Link} from 'react-router-dom';

export default () => {
    return (
        <div>
            <div>DB Editor edits DB</div>
            <Link to={'/'}>Home</Link>
        </div>
    )
}